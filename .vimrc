" Enable line numbers and relative line numbers
set number              " Show absolute line numbers
set relativenumber      " Show relative line numbers

" Enable syntax highlighting and set color scheme
syntax enable           " Enable syntax highlighting
set background=dark     " Set background to dark (you can use light if preferred)

" Set indentation
set expandtab           " Convert tabs to spaces
set tabstop=4           " Set the width of a tab character to 4 spaces
set shiftwidth=4        " Set the width for auto-indentation to 4 spaces
set softtabstop=4       " Set the number of spaces for each tab
set smartindent         " Enable smart indenting
set autoindent          " Enable automatic indentation
set smarttab            " Enable smart tabbing

" Enable line wrapping (optional)
set wrap                " Enable text wrapping (toggle with :set nowrap)

" Show matching parentheses/brackets
set showmatch           " Show matching parentheses, brackets, and braces

" Set search highlighting and incremental search
set incsearch           " Enable incremental search
set hlsearch            " Highlight search matches

" Show line and column in the status line
set ruler               " Show cursor position in the status line

" Set line spacing (optional, for better readability)
set linespace=1         " Increase line spacing slightly


